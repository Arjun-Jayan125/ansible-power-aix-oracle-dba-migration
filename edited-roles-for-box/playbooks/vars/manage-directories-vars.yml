hostname: 129.40.76.242                       # AIX Lpar hostname where the Database is running.
service_name: prod                            # Database service name.
listener_port: 1521                        # Database port number.
directory_name: TESTDIR_THIN                    # Desired directory name to be created.
path: /u01/dbdir2                           # OS Path on AIX Lpar. This path must exist.
state: present                             # To create a directory - present. To drop a directory - absent.
mode: enforce
# oracle_db_home not needed - oracle_directory module uses Thin mode (pure Python, no Oracle client required)
