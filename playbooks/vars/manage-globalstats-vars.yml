hostname: <your_database_host>                 # AIX hostname where the Database is running.
service_name: <your_service_name>              # Database service name.
listener_port: 1521                            # Database port number.
# oracle_db_home not needed - oracle_stats_prefs module uses Thin mode (pure Python, no Oracle client required)
preference_name: CONCURRENT                    # Statistics preference.
preference_value: ALL                          # Value
state: absent
