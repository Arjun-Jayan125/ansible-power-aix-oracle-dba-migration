hostname: <your_database_host>                 # AIX hostname where the Database is running.
service_name: <your_service_name>              # Database service name.
listener_port: 1521                            # Database port number.
# oracle_db_home not needed - oracle_jobclass module uses Thin mode (pure Python, no Oracle client required)
state: absent
resource_group:
class_name: ansiclass
logging: failed runs
history: 14
